apiVersion: v1
kind: Service
metadata:
  name: lifeos-app
  labels:
    app: lifeos-app
    {{- include "lifeos.labels" . | nindent 4 }}
spec:
  selector:
    app: lifeos-app
  ports:
    - name: http
      port: {{ .Values.app.port }}
      targetPort: {{ .Values.app.port }}
