apiVersion: v1
kind: Service
metadata:
  name: n8n
  labels:
    app: n8n
    {{- include "lifeos.labels" . | nindent 4 }}
spec:
  selector:
    app: n8n
  ports:
    - port: 5678
      targetPort: 5678
